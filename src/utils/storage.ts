/**
 * Ephemeral session utility.
 * Pure in-memory automation: intentionally does NOT persist data to disk or chrome.storage.
 */

export async function saveStoredNumbers(): Promise<void> {}

export async function loadStoredNumbers(): Promise<string> {
  return '';
}

export async function clearStoredNumbers(): Promise<void> {}
